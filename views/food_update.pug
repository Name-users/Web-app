doctype html
html
    head
        title= title
        link(rel='stylesheet', href='/stylesheets/style.css')
    body
        h1 Update

        form(method='POST', action=path + '/update', enctype="multipart/form-data")
            li
                label Name:
                    input(name='name', value= name, type='text')
            li
                label #{l2}:
                    input(name= l2.toLowerCase(), value= i2, type='text')
            li
                label #{l3}:
                    input(name=l3.toLowerCase(), value= i3, type='text')
            li
                label Image:
                    input(name='filedata', type='file')
            input(value='Update', type='submit')

        form(method= 'POST', action= path + '/delete')
            input(value='Delete', type='submit')


        // add delete form
