doctype html
html
    head
        title= title
        link(rel='stylesheet', href='/stylesheets/index.css')
        link(rel='stylesheet', href='/stylesheets/tables.css')
    body
        div.background_tables
            h1.title= title
            p.page= page
            p.description= description
             - for (var x = 0; x < tables.length; x++)
                li
                    div.board_on_background
                        if is_admin
                            a(href='/admin/tables/' + tables[x]) Table #{tables[x]}
                        else
                            a(href='/tables/' + tables[x]) Table #{tables[x]}
            if is_admin
                form(method='POST', action='/admin/tables/update')
                    li
                        input.text(name='number', value='', type='text', placeholder="Number")
                    li
                        input.text(name='description', value='', type='text', placeholder="Description")
                    input.submit(value='Create table', type='submit')
            div.board_on_background
                if is_admin
                    a(href='/admin') Back
                else
                    a(href='/') Back
        //img(src='/images/tables.jpg')